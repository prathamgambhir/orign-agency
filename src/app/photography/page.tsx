import { ServicePage } from "@/components/service-page";

export const metadata = { title: "Photography | Orign" };

export default function Page() {
  return <ServicePage serviceKey="photography" />;
}
